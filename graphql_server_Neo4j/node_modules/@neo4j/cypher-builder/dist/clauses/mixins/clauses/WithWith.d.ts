import { With } from "../../..";
import type { WithProjection } from "../../With";
import { MixinClause } from "../Mixin";
export declare abstract class WithWith extends MixinClause {
    /** Add a {@link With} clause
     * @see [Cypher Documentation](https://neo4j.com/docs/cypher-manual/current/clauses/with/)
     */
    with(clause: With): With;
    with(...columns: Array<"*" | WithProjection>): With;
    private getWithClause;
}
